# Blender 施工场景搭建 — 零基础逐步教程

> 目标：15m × 15m 施工工地场景，含基础地面 + 土路捷径 + 水泥绕行路 + 障碍物
> 前提：Blender 已安装并打开（默认启动场景有一个 Cube、一个 Camera、一个 Light）

---

## 准备：清理默认场景 + 设置

### 删除默认方块
1. 鼠标左键点一下画面中间的方块（选中后会有橙色边框）
2. 按键盘 `X`
3. 弹出的菜单选 **Delete**（或直接按 `Enter`）

### 切换到顶视图（做平面布局用）
4. 按键盘 `Numpad 7`（数字键盘的 7，不是上面那排数字）
   - 如果没有数字键盘：按 `~`（波浪号，在 Esc 下面），弹出菜单后选 **Top**
5. 现在你从正上方往下看，画面是平的

### 确认单位是米
6. 看画面左下角的网格线——Blender 默认每格 = 1m，一大格 = 1m。后面做 15m × 15m 场景，就是 15 大格。

---

## 第 1 层：基础地面（粗糙土壤）

### 1.1 创建地面 Plane
1. 把鼠标放在 3D 视图中间
2. 按 `Shift + A`（同时按住 Shift，再按 A）
3. 弹出一个大菜单
4. 鼠标移到 **Mesh** → 再移到 **Plane** → 点击
5. 画面出现一个很小的白色方形

### 1.2 把 Plane 放大到 15m × 15m
6. 确保 Plane 还是选中状态（橙色边框）
7. 按 `S`（Scale，缩放）
8. 输入数字 `15`（你会看到 Plane 变大）
9. 按 `Enter` 确认
   - 现在 Plane 是 15m × 15m

### 1.3 重命名为 BaseGround
10. 看画面**右上角**的列表（叫 Outliner）
11. 找到 **Plane** 这一行
12. 双击 "Plane" 这个名字
13. 输入 `BaseGround` → 按 `Enter`

### 1.4 给地面加粗糙感（让它不再光滑平整）
14. 选中 BaseGround
15. 看画面**右下角**的属性面板
16. 找到那个**蓝色扳手**图标（Modifiers）→ 点击
17. 点击 **Add Modifier** 下拉菜单
18. 找到 **Displace** → 点击
19. 在刚出现的 Displace 设置里：
    - 点击 **New** 按钮（在 Texture 那一栏）
    - 把 **Strength** 从 1.000 改成 **0.400**
    - 把 **Midlevel** 从 0.500 改成 **0.000**
20. 现在去看 3D 视图——地面应该有不规则凹凸了
    - 如果看不出来，按 `Numpad 1` 切换到前视图从侧面看
    - 按 `Numpad 7` 回到顶视图

### 1.5 调粗糙纹理细节
21. 点击画面右下角**红色棋盘格**图标（Texture Properties）
22. 应该看到一个叫 "Texture" 的纹理
23. 把 **Type** 下拉从 "Image or Movie" 改成 **Clouds**（或 **Voronoi** 试试哪个更像施工地面）
24. 调下面 **Size** = `2.00`
25. 调 **Depth** = `3`
26. 回到顶视图（Numpad 7）看效果

**现在你有了：一个 15m × 15m 的粗糙土壤地面。** ✅

---

## 第 2 层：土路捷径（DirtPath）

这条路从 Start 斜穿到 Goal，要从障碍物中间穿过。我们先做一个窄长的 Plane。

### 2.1 创建路径
1. 按 `Shift + A` → Mesh → Plane
2. 按 `S` → 输入 `1.5` → Enter（把宽度缩到 1.5m）
3. 按 `S` → 再按 `Y`（锁定 Y 轴）→ 输入 `6` → Enter（长度拉到 6m）
4. 按 `G` → 移动鼠标把它放到场景中间偏左的位置
5. 按 `G` → 再按 `Z` → 输入 `0.03` → Enter（抬高 3cm，防止和 BaseGround 重叠闪烁）

### 2.2 旋转路径让它呈对角线
6. 按 `R` → 输入 `30` → Enter（旋转 30 度，可以根据你想要的 Start-Goal 方向自己调角度）

### 2.3 重命名
7. 右上角 Outliner 里双击新 Plane → 改成 `DirtPath`

**现在你有了：一条 1.5m 宽的土路捷径。** ✅

---

## 第 3 层：水泥绕行路（ConcretePath）

这条路绕过所有障碍走外围。做 2 段拼成 L 形，或者 3 段拼成 U 形。

### 方法：拼两段 Plane 做 L 形绕行路

### 3.1 第一段（水平段）
1. `Shift + A` → Mesh → Plane
2. `S` → `Y` → `10` → Enter（拉长到 10m）
3. `S` → `X` → `1.5` → Enter（缩窄到 1.5m）
4. `G` → 移动到靠近场景底部边缘
5. `G` → `Z` → `0.03` → Enter（抬高）
6. 重命名为 `ConcretePath_H`

### 3.2 第二段（垂直段）
7. `Shift + A` → Mesh → Plane
8. `S` → `Y` → `8` → Enter
9. `S` → `X` → `1.5` → Enter
10. `R` → `90` → Enter（旋转 90 度，绕 Y 轴做不到？试试 `R` → `Z` → `90` → Enter）
11. `G` → 移动到场景右侧边缘，和第一段首尾相接
12. `G` → `Z` → `0.03` → Enter
13. 重命名为 `ConcretePath_V`

**现在你有了：L 形水泥绕行路（两段总长约 18m）。** ✅

---

## 第 4 层：放 Start 和 Goal 标记

用 Empty 对象做标记（Empty 是不可见的定位点，导入 Isaac Sim 后表示坐标）。

### 4.1 放 Start
1. `Shift + A` → 菜单最下面找到 **Empty** → 选 **Plain Axes**
2. 画面出现一个小的坐标轴
3. `G` → 拖动到 DirtPath 的起点位置
4. 重命名为 `Start`

### 4.2 放 Goal
5. `Shift + A` → Empty → Plain Axes
6. `G` → 拖动到 DirtPath 的终点位置
7. 重命名为 `Goal`

**现在你有了：Start 和 Goal 的定位标记。** ✅

---

## 第 5 层：障碍物（让路径有"穿过 vs 绕行"的选择）

### 障碍物 1：垃圾桶（捷径入口两侧，收窄通道）

1. `Shift + A` → Mesh → **Cylinder**
2. 在左下角弹出的面板里（如果没有弹面板，按 `F9`）：
   - Vertices = `32`
   - Radius = `0.25`
   - Depth = `1.0`
3. `S` → `Z` → `0.8` → Enter（稍微压扁，更像垃圾桶）
4. `G` → 移动到 DirtPath 入口左侧
5. 重命名为 `TrashCan_01`

**复制第二个垃圾桶：**
6. 选中 TrashCan_01
7. `Shift + D` → 鼠标移动 → 放到入口右侧
8. 重命名为 `TrashCan_02`

---

### 障碍物 2：建筑材料堆（捷径中段，木箱/砖块堆）

做 4-5 个大小不同的方块堆在一起。

1. `Shift + A` → Mesh → **Cube**
2. `S` → `X` → `0.8` → `Y` → `0.8` → `Z` → `0.5` → Enter（变长方体，像木板箱）
3. `G` → 移动到 DirtPath 中间附近
4. 重命名为 `MaterialStack_01`

**复制堆叠：**
5. 选中 MaterialStack_01
6. `Shift + D` → 鼠标稍微偏移 → 左键放置
7. `G` → `Z` → `0.5` → Enter（抬高半个箱子，叠在上面）
8. 重命名为 `MaterialStack_02`
9. 重复 `Shift + D` 再复制 2-3 个，分散在周围，有的放地上，有的歪着（`R` 旋转一点）

---

### 障碍物 3：钢筋/管道（细长圆柱，散落地面）

1. `Shift + A` → Mesh → Cylinder
2. 在 `F9` 面板：
   - Radius = `0.05`
   - Depth = `3.0`
3. `R` → `X` → `90` → Enter（横过来，躺在地上）
4. `G` → 移动到 DirtPath 附近地面
5. 重命名为 `Rebar_01`

**复制多根：**
6. `Shift + D` → 移动到旁边 → `R` → `Z` → 随机角度
7. 重复 3-4 次，散落在捷径中段地面

---

### 障碍物 4：简易脚手架（钢管框架）

用细 Cylinder 做竖杆 + Cube 做横板。

**4 根竖杆：**
1. `Shift + A` → Mesh → Cylinder
2. F9：Radius = `0.05`，Depth = `2.5`
3. 重命名为 `Scaffold_Pole_01`
4. `Shift + D` → `G` 移动到 1m 外 → 重命名 `Scaffold_Pole_02`
5. 再 `Shift + D` 两次 → 摆成 4 个角（正方形 1m × 1m）

**2 块横板：**
6. `Shift + A` → Mesh → Cube
7. `S` → `X` → `1.1` → `Y` → `0.3` → `Z` → `0.05` → Enter（薄板）
8. `G` → `Z` → `1.0` → Enter（抬到杆子中间高度）
9. `G` → 放到两根竖杆之间
10. 重命名为 `Scaffold_Plank_01`
11. `Shift + D` → `G` → `Z` → `1.8` → Enter（第二块板抬高到杆子顶部）

**整体移动：**
12. 按住 `Shift`，逐个点击 4 根杆子 + 2 块板（全选）
13. `Ctrl + J`（Join，合并为一个物体，方便后续移动）
14. 重命名为 `Scaffold`
15. `G` → 移动到 DirtPath 后半段

---

### 障碍物 5：水泥搅拌车（大障碍，堵在捷径末端）

用基本几何体拼一个简化的搅拌车。

**车身底盘：**
1. `Shift + A` → Mesh → Cube
2. `S` → `X` → `2.0` → `Y` → `1.0` → `Z` → `0.5` → Enter
3. `G` → `Z` → `0.8` → Enter（抬高离地，给轮子留空间）
4. 重命名为 `Mixer_Body`

**搅拌筒（旋转的桶）：**
5. `Shift + A` → Mesh → Cylinder
6. F9：Radius = `0.5`，Depth = `1.5`
7. `R` → `X` → `80` → Enter（倾斜，像搅拌筒的角度）
8. `G` → 放到车身顶部

**驾驶室：**
9. `Shift + A` → Mesh → Cube
10. `S` → `X` → `0.8` → `Y` → `0.8` → `Z` → `0.8` → Enter
11. `G` → 放到车身前端

**轮子（4 个 Cylinder）：**
12. `Shift + A` → Mesh → Cylinder
13. F9：Radius = `0.3`，Depth = `0.2`
14. `R` → `Y` → `90` → Enter（转成侧向，像轮子）
15. `G` → 放到车身左下角
16. `Shift + D` 复制 3 个，分别放四个角

**合并整车：**
17. 按住 `Shift`，点击车身 + 搅拌筒 + 驾驶室 + 4 个轮子
18. `Ctrl + J`（全部合并）
19. 重命名为 `ConcreteMixer`
20. `G` → 放到 DirtPath 靠近 Goal 的位置，堵住大半个通道

---

## 第 6 层（可选）：给地面多加一些粗糙细节

### 给 BaseGround 手动拉地形

如果你想要**局部控制**某些区域更粗糙（比如障碍物周围有坑洼），在 BaseGround 上手动编辑：

1. 选中 BaseGround
2. `Tab` 进入编辑模式（物体变橙色线框）
3. 右键 → **Subdivide**（细分，增加顶点数）
4. 在左下角 F9 面板：Number of Cuts = `15`
5. 按 `A` 取消全选
6. 鼠标框选障碍物周围的顶点
7. 按 `O` 键（开启 Proportional Editing，屏幕顶部中间会出现一个圆形图标）
8. 滚动鼠标滚轮调整影响范围（圆圈大小）
9. `G` → `Z` → 鼠标上下移动 → 顶点被拉起或压低，周围跟着平滑过渡
10. 在障碍物区域拉高做小土堆，在路径区域压平
11. `Tab` 退出编辑模式

---

## 最终验收清单

打开你的 Blender 文件，在 Outliner（右上角列表）里应该有：

- [ ] `BaseGround` — 15m × 15m 粗糙土壤平面
- [ ] `DirtPath` — 1.5m 宽土路，连接 Start 到 Goal
- [ ] `ConcretePath_H` + `ConcretePath_V` — L 形绕行水泥路
- [ ] `Start` — Empty 标记起点
- [ ] `Goal` — Empty 标记终点
- [ ] `TrashCan_01` + `TrashCan_02` — 捷径入口垃圾桶
- [ ] `MaterialStack_01 ~ 04` — 材料堆
- [ ] `Rebar_01 ~ 04` — 散落钢筋
- [ ] `Scaffold` — 简易脚手架
- [ ] `ConcreteMixer` — 水泥搅拌车

**两条路径的"决策逻辑"：**

| 路径 | 走向 | 难度 |
|------|------|------|
| 捷径 | Start → 挤过垃圾桶 → 绕过材料堆 → 跨过钢筋 → 钻过脚手架 → 擦过搅拌车 → Goal | 难但短 |
| 绕行 | Start → 走外围水泥路 → 避开所有障碍 → Goal | 容易但长 |

---

## 保存文件

1. `Ctrl + S`（保存）
2. 文件名叫 `construction_site.blend`
3. 保存到你能找到的地方（比如桌面或 Documents）

---

## 常用救命操作

| 操作 | 快捷键 |
|------|--------|
| 撤销 | `Ctrl + Z` |
| 重做 | `Ctrl + Shift + Z` |
| 删除选中的 | `X` → Enter |
| 只看选中物体（隔离） | `/`（斜杠） |
| 恢复显示全部 | 再按 `/` |
| 聚焦选中物体 | `Numpad .`（小数点） |
| 顶视图 | `Numpad 7` |
| 前视图（侧面） | `Numpad 1` |
| 右视图 | `Numpad 3` |
| 拉近/拉远 | 鼠标滚轮 |
| 旋转视角 | 鼠标中键拖拽 |
| 平移视角 | `Shift + 鼠标中键` 拖拽 |
| F9 面板（调参数） | 刚创建物体后按 `F9` 可以调参数 |
| 复制物体 | `Shift + D` |
| 合并物体 | 多选后 `Ctrl + J` |
