# Blender 施工场景搭建 — 零基础逐步教程

> 目标：15m × 15m 施工工地场景，含基础地面 + 土路捷径 + 水泥绕行路 + 障碍物
> 前提：Blender 已安装并打开（默认启动场景有一个 Cube、一个 Camera、一个 Light）

---

## 准备：清理默认场景 + 设置

### 删除默认方块
1. 鼠标左键点一下画面中间的方块（选中后会有橙色边框）
2. 按键盘 `X`
3. 弹出的菜单选 **Delete**（或直接按 `Enter`）

### 切换到顶视图（做平面布局用）
4. 按键盘 `Numpad 7`（数字键盘的 7，不是上面那排数字）
   - 如果没有数字键盘：按 `~`（波浪号，在 Esc 下面），弹出菜单后选 **Top**
5. 现在你从正上方往下看，画面是平的

### 确认单位是米
6. 看画面左下角的网格线——Blender 默认每格 = 1m，一大格 = 1m。后面做 15m × 15m 场景，就是 15 大格。

---

## 第 1 层：基础地面（粗糙土壤）

### 1.1 创建地面 Plane
1. 把鼠标放在 3D 视图中间
2. 按 `Shift + A`（同时按住 Shift，再按 A）
3. 弹出一个大菜单
4. 鼠标移到 **Mesh** → 再移到 **Plane** → 点击
5. 画面出现一个很小的白色方形

### 1.2 把 Plane 放大到 15m × 15m
6. 确保 Plane 还是选中状态（橙色边框）
7. 按 `S`（Scale，缩放）
8. 输入数字 `15`（你会看到 Plane 变大）
9. 按 `Enter` 确认
   - 现在 Plane 是 15m × 15m

### 1.3 重命名为 BaseGround
10. 看画面**右上角**的列表（叫 Outliner）
11. 找到 **Plane** 这一行
12. 双击 "Plane" 这个名字
13. 输入 `BaseGround` → 按 `Enter`

### 1.4 细分 Plane（关键！Plane 默认只有 4 个顶点，Displace 没东西可移）
14. 选中 BaseGround
15. 按 `Tab` 进入编辑模式（物体变橙色线框）
16. 鼠标右键 → **Subdivide**
17. 左下角弹出小面板（没弹出就按 `F9`）
18. **Number of Cuts** 改成 `20`（不是 50 或 100，20 就够了，多了会卡退）
19. 按 `Tab` 退出编辑模式
   - 现在 Plane 有 21×21 = 441 个顶点，Displace 可以移动它们产生凹凸了

### 1.5 给地面加粗糙感（Displace 修改器 + 纹理）
20. 选中 BaseGround
21. 看画面**右下角**的属性面板
22. 找到那个**蓝色扳手**图标（Modifiers）→ 点击
23. 点击 **Add Modifier** 下拉菜单
24. 找到 **Displace** → 点击
25. 在刚出现的 Displace 设置里：
    - 点击 **New** 按钮（在 Texture 那一栏）
    - 把 **Strength** 从 1.000 改成 **0.30**（施工地面微粗糙即可）
    - 把 **Midlevel** 从 0.500 改成 **0.000**
26. 现在去看 3D 视图——地面应该有不规则凹凸了
    - 如果看不出来：切前视图 `Numpad 1` 从侧面看
27. 点击画面右下角**红色棋盘格**图标（Texture Properties）
28. 应该看到一个叫 "Texture" 的纹理
    - 如果没有看到：回到 Modifiers 面板确认点了 **New** 按钮
29. 把 **Type** 下拉从 "Image or Movie" 改成 **Clouds**
30. 调下面 **Size** = `2.00`（控制起伏密度，值越大越平缓）
31. 调 **Depth** = `3`（噪声层数，3 层叠加足够）
32. 回到顶视图（Numpad 7）看效果

### 1.6 应用 Displace（把起伏"烧进"网格，否则地面会飞在天上）
33. 选中 BaseGround
34. 右下角蓝色扳手面板（Modifiers）→ 找到 Displace
35. 点 Displace 右边的 **▼ 下拉箭头**
36. 选 **Apply**（应用）
    - 应用后地面位置会变。如果看到地面往上飞了 0.1-0.2m：
      `G` → `Z` → `-0.15` → Enter 手动压回去
    - 也可以跳过 Apply，改为：选中 BaseGround，`G` → `Z` → 上下微调直到和之后的
      路径平面大致接触即可

**验证：** 按 `Numpad 1` 切前视图。BaseGround 应该是紧贴世界原点地面的（Z≈0 附近
有一层微粗糙的起伏），而不是悬浮在半空。

**现在你有了：一个 15m × 15m 的粗糙土壤地面。** ✅

### 1.7 多余纹理清理（如果之前误操作多了纹理）
30. 顶部菜单：**File → Clean Up → Recursive Unused Data-Blocks**
31. 弹窗点 **OK**

---

## 第 2 层：放 Start 和 Goal 标记（先放锚点，路才能对准）

在创建路径之前先放两个 Empty 做定位锚点——这样路径可以直接从 Start 拉到 Goal，
不需要猜角度。

### 2.1 放 Start
1. 按 `Numpad 7` 切换到顶视图
2. 按 `Shift + A` → 菜单最下面找到 **Empty** → 选 **Plain Axes**
   - 画面出现一个很小的三色坐标轴
3. 按 `G` → 鼠标拖动到场景左下角
   建议位置：大约 X=-5, Y=-5（看画面右下角的坐标显示，或看网格线数 5 大格）
4. 右上角 Outliner 里双击 "Empty" → 重命名为 `Start`

### 2.2 放 Goal
5. 按 `Shift + D` → 鼠标拖动到右上角（复制 Start）
   建议位置：大约 X=5, Y=5
6. 重命名为 `Goal`

### 2.3 确保 Start 和 Goal 在同一高度
7. 选中 Start → 按 `G` → `Z` → `0.03` → Enter
8. 选中 Goal → 按 `G` → `Z` → `0.03` → Enter
   （抬高 3cm，之后路也在这个高度，防止和 BaseGround 穿插闪烁）

---

## 第 3 层：土路捷径（DirtPath）

### 3.1 创建路径 Plane
1. 按 `Numpad 7` 顶视图
2. 按 `Shift + A` → Mesh → Plane
3. 右上角 Outliner 双击 "Plane" → 重命名为 `DirtPath`
4. 按 `S` → `X` → `1.5` → Enter（宽度缩到 1.5m）
5. 按 `S` → `Y` → `8` → Enter（长度拉到 8m，先做长点后面裁）
6. 按 `G` → `Z` → `0.03` → Enter（抬高 3cm，和 Start/Goal 同高度）

### 3.2 对齐到 Start（详细旋转+定位步骤）
7. 确保 DirtPath 选中（橙色边框）
8. 按 `G` → 鼠标把 DirtPath 移到 Start 附近（先不做精确对齐）
9. 按 `R`（旋转模式启动）
10. 现在移动鼠标——DirtPath 会跟着旋转
11. 让 DirtPath 的长轴方向大致指向 Start 到 Goal 的连线方向
12. 当方向看起来对时，**左键点击**确认旋转
    - 注意：此时不按 Enter！旋转模式下左键=确认，Enter 也会确认。两个都可以。

    **如果 Start 在左下 (X≈-5, Y≈-5)，Goal 在右上 (X≈5, Y≈5)：**
    对角线刚好是 45°，直接 `R` → `45` → Enter 就能精确对准。

    **如果角度不对想重来：** `Ctrl+Z` 撤销旋转，重新 `R` 微调。

### 3.3 拉伸两端对齐 Start 和 Goal
13. 按 `Tab` 进入编辑模式（DirtPath 变橙色线框，显示顶点）
14. 确认你处于顶点选择模式：
    看屏幕左上角，应该高亮的是 **顶点图标**（最左边那个小方块）
    如果不是就按 `1` 切换到顶点选择
15. 按 `A` 取消选择所有顶点（顶点从橙色变回白色/黑色）
16. 鼠标框选 DirtPath **靠近 Start 那端的两个顶点**
    - 按住鼠标左键拖拽一个矩形框住这两个顶点
    - 选中的顶点会变橙色
17. 按 `G` → 拖动这两个顶点到 **Start 标记上**
    - 可以用滚轮缩放视图来精确定位
    - Start 的坐标轴中心就是精确位置
18. 按 `A` 取消选择
19. 鼠标框选 DirtPath **靠近 Goal 那端的两个顶点**
20. 按 `G` → 拖动到 Goal 标记上
21. 按 `Tab` 退出编辑模式

### 3.4 验证对齐
22. 按住鼠标中键旋转视角到倾斜角度
23. 检查 DirtPath 两端是否分别在 Start 和 Goal 上方
24. 检查 DirtPath 是否被 BaseGround 盖住：
    - 如果看不到路：`/` 斜杠键隔离查看 DirtPath
    - 确认它在 Z=0.03 高度 → 再按 `/` 退出隔离
    - 如果仍然穿插：`G` → `Z` → `0.05` 再抬高一点

**现在你有了：一条精准连接 Start 到 Goal 的 1.5m 宽土路。** ✅

---

## 第 4 层：水泥绕行路（ConcretePath）

这条路绕过所有障碍走外围，拼两段 Plane 做 L 形。

### 4.1 第一段（水平段）
1. 按 `Numpad 7` 顶视图
2. 按 `Shift + A` → Mesh → Plane
3. 右上角 Outliner 双击 "Plane" → 重命名为 `ConcretePath_H`
4. 按 `S` → `Y` → `10` → Enter（拉长到 10m）
5. 按 `S` → `X` → `1.5` → Enter（缩窄到 1.5m）
6. 按 `G` → 鼠标拖动到场景底部边缘（靠近 Y≈-7 的位置）
7. 按 `G` → `Z` → `0.03` → Enter（抬高，和其他路同高度）

### 4.2 第二段（垂直段，拼成 L 形）
8. 按 `Shift + A` → Mesh → Plane
9. 重命名为 `ConcretePath_V`
10. 按 `S` → `Y` → `8` → Enter
11. 按 `S` → `X` → `1.5` → Enter
12. 按 `R` → `90` → Enter（旋转 90°）
    - 如果旋转后方向不是垂直的：`R` → `Z` → `90` → Enter
13. 按 `G` → 鼠标拖动到场景右侧边缘（X≈7 附近）
14. 让它的下端和 ConcretePath_H 的右端接近相接
15. 按 `G` → `Z` → `0.03` → Enter

### 4.3 检查路径连接
16. 按 `Numpad 7` 顶视图
17. 检查 ConcretePath_H 和 ConcretePath_V 的端头是否靠在一起
    - 不需要完美焊接，接近即可——这两段在 Isaac Sim 里都作为可走的路面
18. 如果间隙太大：`G` → 拖动调整位置

**现在你有了：L 形水泥绕行路（两段总长约 18m）。** ✅

---

## 第 5 层：障碍物（让路径有"穿过 vs 绕行"的选择）

### 障碍物 1：垃圾桶（捷径入口两侧，收窄通道）

1. `Shift + A` → Mesh → **Cylinder**
2. 在左下角弹出的面板里（如果没有弹面板，按 `F9`）：
   - Vertices = `32`
   - Radius = `0.25`
   - Depth = `1.0`
3. `S` → `Z` → `0.8` → Enter（稍微压扁，更像垃圾桶）
4. `G` → 移动到 DirtPath 入口左侧
5. 重命名为 `TrashCan_01`

**复制第二个垃圾桶：**
6. 选中 TrashCan_01
7. `Shift + D` → 鼠标移动 → 放到入口右侧
8. 重命名为 `TrashCan_02`

---

### 障碍物 2：建筑材料堆（捷径中段，木箱/砖块堆）

做 4-5 个大小不同的方块堆在一起。

1. `Shift + A` → Mesh → **Cube**
2. `S` → `X` → `0.8` → `Y` → `0.8` → `Z` → `0.5` → Enter（变长方体，像木板箱）
3. `G` → 移动到 DirtPath 中间附近
4. 重命名为 `MaterialStack_01`

**复制堆叠：**
5. 选中 MaterialStack_01
6. `Shift + D` → 鼠标稍微偏移 → 左键放置
7. `G` → `Z` → `0.5` → Enter（抬高半个箱子，叠在上面）
8. 重命名为 `MaterialStack_02`
9. 重复 `Shift + D` 再复制 2-3 个，分散在周围，有的放地上，有的歪着（`R` 旋转一点）

---

### 障碍物 3：钢筋/管道（细长圆柱，散落地面）

1. `Shift + A` → Mesh → Cylinder
2. 在 `F9` 面板：
   - Radius = `0.05`
   - Depth = `3.0`
3. `R` → `X` → `90` → Enter（横过来，躺在地上）
4. `G` → 移动到 DirtPath 附近地面
5. 重命名为 `Rebar_01`

**复制多根：**
6. `Shift + D` → 移动到旁边 → `R` → `Z` → 随机角度
7. 重复 3-4 次，散落在捷径中段地面

---

### 障碍物 4：简易脚手架（钢管框架）

用细 Cylinder 做竖杆 + Cube 做横板。

**4 根竖杆：**
1. `Shift + A` → Mesh → Cylinder
2. F9：Radius = `0.05`，Depth = `2.5`
3. 重命名为 `Scaffold_Pole_01`
4. `Shift + D` → `G` 移动到 1m 外 → 重命名 `Scaffold_Pole_02`
5. 再 `Shift + D` 两次 → 摆成 4 个角（正方形 1m × 1m）

**2 块横板：**
6. `Shift + A` → Mesh → Cube
7. `S` → `X` → `1.1` → `Y` → `0.3` → `Z` → `0.05` → Enter（薄板）
8. `G` → `Z` → `1.0` → Enter（抬到杆子中间高度）
9. `G` → 放到两根竖杆之间
10. 重命名为 `Scaffold_Plank_01`
11. `Shift + D` → `G` → `Z` → `1.8` → Enter（第二块板抬高到杆子顶部）

**整体移动：**
12. 按住 `Shift`，逐个点击 4 根杆子 + 2 块板（全选）
13. `Ctrl + J`（Join，合并为一个物体，方便后续移动）
14. 重命名为 `Scaffold`
15. `G` → 移动到 DirtPath 后半段

---

### 障碍物 5：水泥搅拌车（大障碍，堵在捷径末端）

用基本几何体拼一个简化的搅拌车。

**车身底盘：**
1. `Shift + A` → Mesh → Cube
2. `S` → `X` → `2.0` → `Y` → `1.0` → `Z` → `0.5` → Enter
3. `G` → `Z` → `0.8` → Enter（抬高离地，给轮子留空间）
4. 重命名为 `Mixer_Body`

**搅拌筒（旋转的桶）：**
5. `Shift + A` → Mesh → Cylinder
6. F9：Radius = `0.5`，Depth = `1.5`
7. `R` → `X` → `80` → Enter（倾斜，像搅拌筒的角度）
8. `G` → 放到车身顶部

**驾驶室：**
9. `Shift + A` → Mesh → Cube
10. `S` → `X` → `0.8` → `Y` → `0.8` → `Z` → `0.8` → Enter
11. `G` → 放到车身前端

**轮子（4 个 Cylinder）：**
12. `Shift + A` → Mesh → Cylinder
13. F9：Radius = `0.3`，Depth = `0.2`
14. `R` → `Y` → `90` → Enter（转成侧向，像轮子）
15. `G` → 放到车身左下角
16. `Shift + D` 复制 3 个，分别放四个角

**合并整车：**
17. 按住 `Shift`，点击车身 + 搅拌筒 + 驾驶室 + 4 个轮子
18. `Ctrl + J`（全部合并）
19. 重命名为 `ConcreteMixer`
20. `G` → 放到 DirtPath 靠近 Goal 的位置，堵住大半个通道

---

## 第 6 层（可选）：给地面多加一些粗糙细节

### 给 BaseGround 手动拉地形

如果你想要**局部控制**某些区域更粗糙（比如障碍物周围有坑洼），在 BaseGround 上手动编辑：

1. 选中 BaseGround
2. `Tab` 进入编辑模式（物体变橙色线框）
3. 右键 → **Subdivide**（细分，增加顶点数）
4. 在左下角 F9 面板：Number of Cuts = `15`
5. 按 `A` 取消全选
6. 鼠标框选障碍物周围的顶点
7. 按 `O` 键（开启 Proportional Editing，屏幕顶部中间会出现一个圆形图标）
8. 滚动鼠标滚轮调整影响范围（圆圈大小）
9. `G` → `Z` → 鼠标上下移动 → 顶点被拉起或压低，周围跟着平滑过渡
10. 在障碍物区域拉高做小土堆，在路径区域压平
11. `Tab` 退出编辑模式

---

## 最终验收清单

打开你的 Blender 文件，在 Outliner（右上角列表）里应该有：

- [ ] `BaseGround` — 15m × 15m 粗糙土壤平面
- [ ] `DirtPath` — 1.5m 宽土路，连接 Start 到 Goal
- [ ] `ConcretePath_H` + `ConcretePath_V` — L 形绕行水泥路
- [ ] `Start` — Empty 标记起点
- [ ] `Goal` — Empty 标记终点
- [ ] `TrashCan_01` + `TrashCan_02` — 捷径入口垃圾桶
- [ ] `MaterialStack_01 ~ 04` — 材料堆
- [ ] `Rebar_01 ~ 04` — 散落钢筋
- [ ] `Scaffold` — 简易脚手架
- [ ] `ConcreteMixer` — 水泥搅拌车

**两条路径的"决策逻辑"：**

| 路径 | 走向 | 难度 |
|------|------|------|
| 捷径 | Start → 挤过垃圾桶 → 绕过材料堆 → 跨过钢筋 → 钻过脚手架 → 擦过搅拌车 → Goal | 难但短 |
| 绕行 | Start → 走外围水泥路 → 避开所有障碍 → Goal | 容易但长 |

---

## 保存文件

1. `Ctrl + S`（保存）
2. 文件名叫 `construction_site.blend`
3. 保存到你能找到的地方（比如桌面或 Documents）

---

## 常用救命操作

| 操作 | 快捷键 |
|------|--------|
| 撤销 | `Ctrl + Z` |
| 重做 | `Ctrl + Shift + Z` |
| 删除选中的 | `X` → Enter |
| 只看选中物体（隔离） | `/`（斜杠） |
| 恢复显示全部 | 再按 `/` |
| 聚焦选中物体 | `Numpad .`（小数点） |
| 顶视图 | `Numpad 7` |
| 前视图（侧面） | `Numpad 1` |
| 右视图 | `Numpad 3` |
| 拉近/拉远 | 鼠标滚轮 |
| 旋转视角 | 鼠标中键拖拽 |
| 平移视角 | `Shift + 鼠标中键` 拖拽 |
| F9 面板（调参数） | 刚创建物体后按 `F9` 可以调参数 |
| 复制物体 | `Shift + D` |
| 合并物体 | 多选后 `Ctrl + J` |

---

## 常见踩坑（新手必看）

### Displace 后地面还是平的？
**根因**：Plane 默认只有 4 个顶点，Displace 没顶点可移。
**解决**：`Tab` → 右键 Subdivide → Number of Cuts = `20` → `Tab` 退出。Displace 立刻生效。

### Blender 卡死/崩溃
**根因**：Subdivide Cuts 设太大（如 100），顶点数爆炸（10000+）。
**解决**：Cuts = `20` 就够，15m×15m 施工地面不需要高精度网格。

### 起伏太大像山脉
**根因**：Displace Strength 默认 1.0。
**解决**：降到 `0.30`。施工地面是微粗糙，不是山地。

### 纹理改了没反应
**根因**：Blender 的 Texture 面板改了参数但 Displace 没刷新。
**解决**：回到 Modifiers 面板，先关掉 Displace（点眼睛图标），再打开。或者切一下编辑模式 `Tab` → `Tab`。

### 不小心创建了多余纹理删不掉
**解决**：`File → Clean Up → Recursive Unused Data-Blocks`，一键清除所有未引用的纹理/材质。

### 物体移动/缩放过不了被关在奇怪方向
**根因**：不小心按了 `G`/`S` 后锁定了轴（比如 `G → X` 只能 X 轴移动）。
**解决**：右击取消当前操作，重新 `G` 自由移动。或 `Alt+G` 清空位移、`Alt+S` 清空缩放重来。

### BaseGround 飞在天上，路径没法重叠
**根因**：Displace 修改器把顶点往外推了，但物体坐标还显示 XYZ=(0,0,0)。这是"修改前坐标≠修改后位置"导致的。
**解决**：选中 BaseGround → Modifiers 面板 → Displace 右边 ▼ → Apply。应用后地面回到真实位置。如果还偏高，`G` → `Z` → 负值微调。

### 路径被地形盖住看不到
**根因**：路径和 BaseGround 的 Z 高度重叠，BaseGround 的起伏把路径埋了。
**解决**：选中路径 → `G` → `Z` → `0.05` 或更高 → Enter。按 `/` 隔离查看确认路径本身是好的。
